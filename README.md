## Installation

```terminal

npm install --legacy-peer-deps --force typeorm@0.3.20 mongodb@5.9.2 passport-google-oauth20 @nestjs/passport @nestjs/config

.env irei enviar por email
note: execute o .env no local raiz, ou seja na mesma hierarquia do src e do package.json

## Running the app

$ npm run start
